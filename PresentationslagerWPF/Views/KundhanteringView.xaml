<UserControl x:Class="PresentationslagerWPF.Views.KundhanteringView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PresentationslagerWPF.Views"
        mc:Ignorable="d"
        Height="450" Width="800">
    <StackPanel Margin="10,10,10,10" Background="Gray">
        <StackPanel VerticalAlignment="Top" Background="Gray">
            <Label>KUNDHANTERING</Label>
            <Label>Sök</Label>
            <DockPanel>
                <TextBlock></TextBlock>
            </DockPanel>
        </StackPanel>
        <DockPanel Margin="10,10,10,10" Background="Gray">
            <DataGrid>

            </DataGrid>
        </DockPanel>
        <StackPanel></StackPanel>
        <Button>Add</Button>
        <Button>Ändra</Button>
        <Button>TaBort</Button>
    </StackPanel>
</UserControl>
